/**
 * 计算输入字符串的MD5哈希值
 *
 * @param {string} val - 需要计算哈希值的字符串
 * @returns {string} 返回计算得到的MD5哈希值
 */
export declare function md5(val: string): any;
/**
 * 计算输入字符串的SHA-256哈希值
 *
 * @param {string} val - 需要计算哈希值的字符串
 * @returns {string} 返回计算得到的SHA-256哈希值
 */
export declare function sha256(val: string): any;
